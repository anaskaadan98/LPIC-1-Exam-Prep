`man`, `--help` or `-h`

You can learn about all the commands in a Linux OS buy using the `man` command

```bash
man + <command>
```

A manual page will be loaded that will talk about the command in detail.

Some commands come with the option `--help` or `-h`

```bash
<command> + --help
<command> + -h
```

---
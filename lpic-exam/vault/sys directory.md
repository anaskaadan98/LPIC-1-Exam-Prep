The `sysfs`: A virtual file system that exports information about the kernel's device model, hardware devices, and drivers from kernel space to user space.

- This is the directory were ([[Hardware Abstraction Layer]]) keeps it's database of the devices that are connected to the computer
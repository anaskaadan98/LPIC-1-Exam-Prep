It is a program stored in ROM chip on the motherboard.

The main job is to wake up and test the hardware when you turn on your PC and then hand it over to the OS.

In order to enter the BIOS we can press `Del`, `F2` or `F12` during startup.